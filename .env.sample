# Bot Credentials (Get from https://app.towns.com/developer)
APP_PRIVATE_DATA=your_app_private_data_here
JWT_SECRET=your_jwt_secret_here

# Server Configuration
PORT=5123
BASE_URL=https://your-bot-domain.com

# Miniapp URL (must match BASE_URL in production)
MINIAPP_URL=https://your-bot-domain.com/miniapp.html

# Membership NFT contract address (used to find airdrop recipients)
AIRDROP_MEMBERSHIP_NFT_ADDRESS=0x...

# Optional: comma-separated addresses to exclude from airdrops
# AIRDROP_EXCLUDE_ADDRESSES=0x...,0x...

# Tax system: holder tax + admin tax
# NFT contract whose holders receive the holder tax (required to enable holder tax)
AIRDROP_TAX_NFT_ADDRESS=0x...
# Holder tax percentage (default: 2)
AIRDROP_TAX_PERCENT=2

# Admin tax: sent to the bot admin wallet (smart wallet address)
BOT_ADMIN_ADDRESS=0x...
# Admin tax percentage (default: 1)
AIRDROP_ADMIN_TAX_PERCENT=1

# Admin rights: userId (EOA) that can cancel airdrops, reset leaderboard/history
ADMIN_RIGHTS=0x...

# Database path (Render persistent disk)
# Defaults to /var/data/airdrop-bot.db in production
# DB_PATH=/var/data/airdrop-bot.db

# Optional: Bot identity image
# BOT_IMAGE_URL=https://your-domain.com/bot-avatar.png
